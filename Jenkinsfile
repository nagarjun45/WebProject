pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'sudo su'
                sh 'cp index.html /var/www/html/WebProject/' 
            }
        }
    }
}